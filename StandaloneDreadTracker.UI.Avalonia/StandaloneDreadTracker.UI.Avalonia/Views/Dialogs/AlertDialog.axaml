<Window xmlns="https://github.com/avaloniaui"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		mc:Ignorable="d"
		x:Class="StandaloneDreadTracker.UI.Avalonia.Views.Dialogs.AlertDialog"
		Name="Dialog"
		WindowStartupLocation="CenterOwner"
		Classes="Dialog NoControlButtons MicaWindow"
		SizeToContent="WidthAndHeight"
		CanResize="False"
		ExtendClientAreaTitleBarHeightHint="-1"
		MinWidth="400">
	<Grid RowDefinitions="Auto,*">
		<Border Grid.Row="0" Classes="TitleBar">
			<TextBlock />
		</Border>

		<Grid Grid.Row="1" RowDefinitions="*,Auto" Margin="20,10" MaxWidth="600">
			<TextBlock Grid.Row="0" Text="{Binding #Dialog.Message}" TextWrapping="Wrap" />

			<StackPanel Grid.Row="1" Orientation="Horizontal" Classes="ButtonContainer">
				<Button Content="{Binding #Dialog.ButtonText}" IsDefault="True" Command="{Binding #Dialog.OnButtonClicked}" />
			</StackPanel>
		</Grid>
	</Grid>
</Window>